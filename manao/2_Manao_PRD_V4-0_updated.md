

# 码瑙——AI 云端代码开发实验室——基于 K8S 的智能开发与实验平台

* **版本：v4.0**
* **时间：2026年5月12日**

---

# 1，项目概述

## 1.1 项目背景

* 现在的编程初学者，要开始学习编程前通常都要在本地机器上配置运行环境，安装依赖，配置各种中间件，添加环境变量等等复杂的操作，新手小白在这一过程中，经常会遇到莫名其妙的报错，由于被墙拉不下来包，找不到安装位置，忘记配置必要的依赖等等，这些**高门槛往往最容易打击初学者**的学习兴趣

* 有些时候自己忘记带笔记本出门，或者相关的开发环境和IDE并没有安装在本机上，但是又**遇到临时需要演示代码的情况**，网上的在线编辑器又通常质量参差不齐，导致效果不尽人意

* 在教学与竞赛环境中，一个长期痛点是：很难保证所有用户处于统一、可控的实验环境中。即便课程内容相同，不同终端、不同依赖、不同系统条件，也会导致运行结果不一致。这会导致：**教学演示难以复现**，**竞赛或实验环境难以统一**，**运维与技术支持需要高投入**等等

## 1.2 项目建设的意义

基于以上痛点，本项目提出“AI 云端开发实验室”的建设构想，旨在打造一个面向初学者、教师、竞赛团队和开发实践场景的智能化云端开发平台。

该平台的核心意义在于：
**把传统的“学代码、配环境、查资料、跑程序、看报错”这一系列分散行为，整合为一个统一、可执行、可反馈、可追踪的在线开发闭环。**

用户只需打开浏览器，即可完成：

* 选择容器并创建项目
* 一键分配运行环境
* 在线编写和管理代码
* 调用 AI 完成解释、补全、报错分析和修复建议。
* 查看日志、运行结果和资源占用
* 根据 AI 分析继续修改和优化

相比传统平台，这种模式将**内容学习**升级为**智能实践**，将**静态平台**升级为**动态实验室**。

同时，也意味着初学者不再需要先掌握复杂的软件安装与环境配置流程，只需要登录平台，就能直接进入标准化的开发环境。
小白可以把更多的注意力放在：

* 语法理解
* 编程思维训练
* 调试能力培养
* 项目实战练习

而不再是环境反复折腾到红温

同时，本平台可以让初学者得到即时反馈，平台接入 AI 后，初学者写完代码不用再停留在**报错了再去搜**。他们可以直接获得：

* 代码含义解释
* 报错原因分析
* 修改建议
* 优化思路
* 示例补全

这种即时反馈机制比传统问答式学习更具陪伴感，也更能提升学习效率。

除了面向初学者，这个平台对于教师和比赛项目也同样有较强价值。

（1）**便于统一教学环境。** 教师可以直接发布标准化实验模板，学生进入后即可开始操作，无需逐个指导环境安装问题。
**这会显著降低教学组织成本，并提升课堂效率。**

（2）便于开展实践型竞赛项目

比赛中，如果一个项目只是“AI 对话网页”，往往**难以体现系统能力**；
而这个项目能够展示：

* 前端交互能力
* 后端业务编排能力
* 云端容器调度能力
* AI 辅助能力
* 监控与运维能力

整体技术表达更完整，更适合作为综合类比赛项目。

## 1.3 产品定位思路

“AI 云端开发实验室”既可以服务于高校竞赛、程序设计实验、课程实践，也可以服务于开发者训练营、企业内部演练平台，还可以作为初学者起步的摇篮
核心是把 AI 和云端执行环境结合起来，形成一个完整闭环：

用户提出需求
→ 平台分配容器实例
→ AI 生成代码或给出修改建议
→ 代码在云端执行
→ 返回日志、结果、资源占用
→ AI 根据运行结果继续解释、调优和修复

这样，平台就不是普通 AI Web，也不是普通在线代码编辑器，而是一个具备以下能力的一体化平台：

1. 可开发：支持多语言项目编辑与管理。
2. 可运行：支持云端容器化执行与环境隔离。
3. 可分析：支持日志、评测、资源数据和 AI 联动分析。
4. 可管理：支持模板、配额、镜像、用户、权限和审计管理。
5. 可展示：适合用于课程实践、竞赛路演和实验教学成果展示。

## 1.4 目标用户与角色

### 1.4.1 目标用户

1. 编程初学者：需要低门槛上手和即时反馈。
2. 教师/助教：需要统一实验模板、查看学生运行记录、管理课堂资源。
3. 竞赛团队：需要展示系统综合能力和平台完整度。
4. 平台管理员：需要维护镜像模板、资源池、用户配额和平台安全。

### 1.4.2 角色定义

#### 普通用户

1. 注册、登录、创建项目。
2. 选择模板、编辑代码、运行任务。
3. 使用 AI 助手获取建议。
4. 查看日志、结果、资源占用和历史记录。

#### 团队所有者

在普通用户能力基础上，额外支持：
1. 管理团队成员
2. 发布实验任务。
3. 分配团队可用容器。
4. 上传资料
5. 查看各个成员的实验执行记录与结果概览。

#### 平台管理员

在团队所有者能力基础上，额外支持：

1. 管理用户和权限，可以强制退出用户
2. 管理运行镜像、模板和配额策略。
3. 查看集群状态、监控面板和审计日志。
4. 配置 AI 服务、消息队列和运行规则。

# 2，系统整体功能

新平台总体可分为五大功能域：

## 2.1 用户与项目管理

支持注册登录、项目创建、团队管理、实验模板绑定、个人资源配额查看。

## 2.2 AI 开发辅助

支持自然语言生成代码、解释代码、分析报错、生成测试用例、优化性能建议。

## 2.3 云端运行与实验

支持一键运行、环境选择、容器实例创建、运行输出。

## 2.4 运维与监控

支持实例状态监控、资源占用查看、任务队列监控、异常告警、日志检索、镜像模板管理。

## 2.5 管理后台

支持用户管理、模板管理、镜像管理、资源池管理、审计日志、运行统计。

# 3，系统设计、技术选型与技术路线

## 3.1 平台整体层次思路

本项目整体采用三层：

第一层是**前端交互层**，负责代码编辑、AI 对话、运行控制、结果展示、资源可视化和用户操作入口。

第二层是**后端业务层**，负责用户认证、项目管理、团队管理、AI 调用编排、任务调度、运行记录、评测系统、文件管理和日志聚合。

第三层是**云端执行层**，负责容器实例创建、镜像管理、资源分配、Pod 生命周期管理、弹性伸缩、监控告警和自动部署。

**把业务逻辑和云上算力调度解耦，让后端按需动态使用云资源，并保证稳定性与高可用性。**

## 3.2 详细业务实现架构层次

**第一层：用户接入层**
Web 浏览器、注册登录

**第二层：前端展示层**
开发工作台、AI 面板、项目管理、团队管理、后台管理页

**第三层：网关与鉴权层**
API Gateway、JWT 鉴权、限流、审计、统一异常处理

**第四层：业务服务层**
用户服务、项目服务、文件服务、AI 编排服务、任务调度服务、评测服务、日志服务、管理服务

**第五层：数据与缓存层**
用于储存容器、pod数据，用户信息，文件，日志，AI对话信息，消息队列等

**第六层：云端执行层**
承载整个集群运行的核心，兼具容器调度、资源管理、日志管理、监控告警等功能。

## 3.3 页面建设思路

新项目暂定设计以下核心页面：

* 用户欢迎页
* 登录与注册页（含"忘记密码"流程）
* 首页
* 用户资源管理页包括：
  用户自身的项目管理页，团队管理页，用户的容器管理页
* 开发工作页包含：
  AI 对话与报错分析页，
  运行结果与日志页
* 通知中心
* 用户设置页
* 集群资源监控页（使用kubersphere与若依框架自带的管理页面）
* 后台管理页

## 3.4 UI 设计思路

原项目 UI 重点是管理端、学生端、教师端的可视化呈现。
新项目 UI 应重点突出“开发者工作台”风格，整体界面建议采用：

* 左侧：项目、文件树、实验模板
* 中间：代码编辑器 / 笔记面板
* 右侧：AI 助手、报错分析、运行控制
* 底部：日志流、资源占用、评测结果、容器状态

视觉风格偏深色、科技感、控制台式布局，更适合比赛展示。

***UI设计部分仅供参考，未定稿！！***

## 3.5 数据与存储思路

原项目以 MySQL 为主数据库，适合课程、用户、作业等结构化业务。
新项目中数据类型明显更复杂，因此我建议改成**混合存储方案**：

* **MySQL**：存用户、权限、项目、实验模板、任务主表、计费或配额等强结构化数据
* **MongoDB**：存 AI 对话记录、运行快照、代码版本、日志摘要、评测明细、实验配置 JSON
* **Redis**：存会话、队列、限流、任务状态缓存、热点配置
* **对象存储服务**： 存大文件，如用户上传的PDF，视频，图片等数据
**核心关系数据继续用关系库，变化快、结构不固定的数据交给文档库。**

## 3.6 数据库逻辑设计思路

建议将数据库逻辑分为以下几类实体：

* 用户表
* 角色权限表
* 项目表
* 项目文件表
* 实验模板表
* 镜像模板表
* 运行任务表
* 运行实例表
* 资源配额表
* AI 对话会话表
* AI 消息明细表
* 运行日志摘要表
* 评测结果表
* 审计日志表

## 3.7 云端执行思路

* 一主节点控制平面
* 多工作节点执行池
* 镜像仓库存标准模板
* 命名空间隔离不同业务域
* Ingress 暴露统一访问入口
* Prometheus + Grafana 做监控
* Loki 做日志聚合
* Helm 管理部署
* KubeSphere 负责可视化运维
* DevOps 负责持续集成与部署

运行时的典型链路如下：

用户点击“运行”
→ 前端提交任务
→ 网关转发到任务调度服务
→ 调度服务检查配额、模板、镜像
→ 调用 Kubernetes Client 创建 Pod/Job
→ Pod 挂载代码与配置
→ 执行完成后将结果写回 MinIO / MongoDB
→ 日志流通过 WebSocket 返回前端
→ AI 编排服务读取结果并给出分析建议

**后端动态分配实例、统一适配镜像、借助 DevOps 完成滚动更新，并通过 K8S 保证高可用。**

新项目继续沿用这个思路，但把执行对象从“课程实验环境”拓展为“开发运行环境”：

* Python 环境
* Java / Maven 环境
* Node.js 环境
* Go 环境
* docker环境
* Rust 环境
* Mysql 环境等

每类环境都封装成标准镜像模板，由平台统一管理。用户发起运行时，系统根据模板、资源配额和任务类型自动创建 Pod 或 Job。

## 3.8 技术路线

在新项目中：

**前端：**
Vue3 + TypeScript + Vite + Pinia + Vue Router + Element Plus + Monaco Editor + ECharts + Axios + WebSocket + xterm

**后端：**
Spring Boot + Spring Cloud +Springboot Alibaba （Gateway + Nacos） + Redis + MySQL + MongoDB + RabbitMQ + Docker Java Client + Kubernetes Java Client

**云端：**
Jenkins + Docker + Kubernetes + Helm + KubeSphere + Metrics Server + Prometheus + Grafana + Loki + Devops

## 3.9 技术选型

### 3.9.1 前端技术选型

**Vue3**
保留。原项目已经验证了 Vue3 在这类平台场景中的可用性，适合做复杂控制台和组件化页面。

**TypeScript**
保留。因为项目前端状态复杂，涉及编辑器、任务状态、资源信息、AI 消息、多角色权限，强类型能明显降低 bug。

**Element Plus**
保留。适合快速搭建后台、表单、抽屉、对话框、表格和控制台页面。

**Monaco Editor**
新增。替代 xterm.js 成为主工作区核心，支持代码高亮、自动补全、错误标注。

**ECharts**
新增。用于展示 CPU、内存、任务趋势、成功率、用户活跃度等可视化内容。

**WebSocket**
强化使用。用于实时日志流、运行状态流和 AI 实时反馈。

### 3.9.2 后端技术选型

**Spring Boot / Spring Cloud Alibaba**
保留微服务思路，但建议从传统 SpringCloud 侧重转到更适合国内比赛项目常见组合的 Spring Cloud + Spring Cloud Alibaba。
理由是：网关、注册配置、限流熔断、服务治理更集中，开发成本更低。

**Nacos**
保留。原项目已采用 Nacos 做注册与配置管理，这一点无需更改。

**Redis**
保留。用于缓存、分布式锁、任务状态、会话和调度队列。

**MySQL**
保留，但不再独占主库职责，只承担核心结构化数据。

**MongoDB**
新增。用于保存对话、代码快照、运行日志摘要、实验配置和 AI 上下文。

**RabbitMQ**
新增。用于异步任务解耦，例如：提交运行、日志处理、评测、AI 后处理。


**对象存储服务**
用于存储大文件，如用户上传的PDF，视频，图片等数据，并且保证存储的稳定性

### 3.9.3 云端技术选型

**Docker**
保留。继续作为标准化运行环境载体。原项目的技术创新点之一就是以 Docker 容器作为资源载体。

**Kubernetes**
保留。仍然是整个平台的核心。原项目强调 K8S 在自动部署、扩缩容、高可用、容错和监控方面的价值，新项目同样依赖这些能力。

**Helm**
保留。负责应用模板化部署。

**KubeSphere**
保留。适合做比赛展示，界面友好，便于演示集群管理、应用发布和资源监控。原项目也是用它作为集群管理平台。

**Metrics Server**
保留。作为基础资源监测来源。原项目明确使用它监测实例状态。

**Prometheus + Grafana**
新增。相比只看 Metrics Server，更适合完整展示监控大屏、告警与趋势图。

**Loki**
新增。用于容器日志聚合，前端查看日志更方便。

**CI/CD**
保留并增强。可用 Jenkins，也可以用 GitLab CI；如果追求轻量，可直接选 Gitea Actions 或 Drone。

## 3.10 功能模块类别及其实现

### 一、用户与权限模块

#### 功能说明

1. 支持注册、登录、退出登录。
2. 支持基于角色的页面和接口权限控制。
3. 支持展示当前用户资源占用情况。

#### 关键要求

1. 本平台计划采用邮箱注册登录。注册时要求用户输入昵称，密码，密码二次确认，邮箱地址，邮箱验证码，图形验证码
2. 密码策略:最小长度、复杂度要求、加盐哈希(BCrypt/Argon2),禁止明文存储
3. Token 方案明确(JWT),含过期时间、刷新机制、注销失效三件套
4. 角色与权限解耦:角色绑定权限点,接口绑定权限点,而非直接 if-else 判断角色
5. 权限变更应即时生效(踢出当前 Token 或刷新声明),不能等下次登录

### 二、团队管理实现模块

#### 功能说明

1. 支持团队管理，团队成员管理。
2. 发布实验项目及分配可用容器。
3. 所有者可上传相关参考资料，成员可以下载。

#### 关键要求

1. 一个团队下面可以创建多个任务，每个任务里面可以分配若干个容器，团队内的成员可同时参与团队里的多个任务，可用容器分配按照任务进行，在任务里规定参与这个项目的人都能用什么容器，只规定可用范围，不做数量约定，由成员自行在可用范围内创建容器。
2. 每个成员在团队里创建的容器互相独立，本平台创立的初衷不包含协作功能，各自的工作区都是独立的；容器可以由团队管理员设置生命周期，暂不考虑容器不足时的情况。
3. 单个参考资料上传时大小不能超过2.0g，一次上传数量不超过15个，不支持版本覆盖，不支持保留上传记录，资料删除后不可恢复
4. 成员可以自行退出团队，也可以由团队所有者踢出团队。
5. 任务由团队所有者关闭后，成员无法再次访问，同时所有分配到这个任务下的容器也会被删除

### 三、项目与文件模块

#### 功能说明
1. 本模块用于实现个人用户和团队用户之间的项目管理功能与工程文件管理功能。
2. 支持创建项目、删除项目、重命名项目。
3. 支持目录树浏览、文件创建、编辑、保存和删除。
4. 支持记录项目更新时间。

#### 关键要求

1. 每个用户新建一个项目相当于创建一个仓库一样（就和在idea里新建一个项目一样），同时要自动初始化git（git只用来做本地的版本管理，不使用推送到远程仓库的功能）
2. 项目分两类:个人项目(归属 user_id)与团队项目(归属 team_id),数据模型中必须区分
3. 团队项目继承团队角色权限(所有者/管理员可读可写,普通成员按授权可读/可写，个人项目只有自己可读可写)、
4. 用户在线时，先临时将代码文件挂载到本地的硬盘上，退出登陆之后再将文件上传到oss

### 四、AI 开发辅助模块

#### 功能说明

1. 支持自然语言问答。
2. 支持代码解释、代码补全建议和优化建议。
3. 支持基于报错日志的原因分析与修复建议。
4. 支持基于运行结果进行二次追问。

#### 关键要求

1. AI 不直接修改用户代码，默认以建议形式输出。
2. AI 响应需绑定当前项目、当前文件、当前运行结果等上下文。
3. 需保留 AI 会话记录，支持后续追溯。
4. 管理员可配置模型路由、Prompt 模板和调用额度。

### 五、日志与结果模块

#### 功能说明

1. 支持实时日志流展示。
2. 支持展示运行输出、错误信息、退出码。
3. 支持保存历史运行记录。
4. 支持基础结果展示，如耗时、资源占用。

#### 关键要求

1. 日志展示需支持实时刷新和历史查询。
2. 运行结果需与任务、项目、用户一一关联。
3. 可为后续自动评测与报告导出预留结构。


### 六、模板与镜像模块

#### 功能说明

1. 支持维护实验模板和项目模板。
2. 支持维护运行镜像模板。
3. 支持模板绑定默认资源配额、启动命令和环境变量。

#### 关键要求

1. 模板要支持启用、停用、版本管理。
2. 模板发布需有审核或管理员确认机制。
3. 镜像与模板的对应关系要清晰可配置。

### 七、监控与运维模块

#### 功能说明

1. 支持查看实例运行状态、CPU、内存等资源指标。
2. 支持查看任务队列积压和执行分布。
3. 支持异常告警和审计追踪。
4. 支持查看集群与业务层面的关键监控信息。

#### 关键要求

1. 平台级监控与用户级监控要分层展示。
2. 审计日志至少覆盖登录、模板发布、任务执行、资源回收等关键操作。
3. 运维模块优先满足教学和竞赛展示需求，再逐步完善企业级能力。

### 八、用户设置与账号安全模块

#### 功能说明

1. 支持用户修改个人资料（昵称、头像）。
2. 支持修改密码（验证旧密码 → 设置新密码）。
3. 支持自定义编辑器偏好：字号（12-24px）、缩进大小（2/4）、主题模式（深色/浅色）、快捷键绑定方案（默认/VSCode/WebStorm）。
4. 支持设置通知偏好开关：任务完成通知、容器过期提醒、AI 分析完成通知、团队动态通知。
5. 支持"忘记密码"流程：登录页入口 → 邮箱验证码验证 → 重置新密码 → 跳转回登录。

#### 关键要求

1. 用户设置页入口位于顶栏用户头像下拉菜单中。
2. 偏好设置修改后自动保存（防抖 1s），无需额外确认按钮。
3. 密码修改需遵守与注册一致的密码策略（最小长度、复杂度、BCrypt 加密）。
4. 各用户的编辑器偏好独立存储，不影响其他用户。
5. 忘记密码流程需校验邮箱验证码，防止恶意重置。

### 九、通知中心模块

#### 功能说明

1. 顶栏右侧展示铃铛图标，有未读通知时图标右上角显示红点及未读数量。
2. 下拉通知列表显示最近 20 条通知，支持滚动加载更多。
3. 支持多种通知类型：任务运行完成、AI 分析完成、容器即将过期（超时前 5 分钟推送）、团队成员变动。
4. 点击单条通知跳转到对应页面（运行记录、容器列表等）。
5. 支持标记单条已读、全部标记已读、清空前端通知列表。
6. 无通知时显示"暂无通知"图标和提示文字。

#### 关键要求

1. 通知数据持久化到数据库，仅清空/标记操作为前端展示层行为。
2. 通知实时性通过 WebSocket 推送保证，不依赖用户主动刷新。
3. 通知与用户 ID 绑定，不同用户互不可见。
4. 通知中心入口不占用额外页面路由，作为全局组件嵌入顶栏。

## 3.11 空状态设计

系统内以下关键页面需设计空状态引导，以提升用户首次使用体验：

### 3.11.1 无项目空状态

用户从未创建项目时，项目列表页居中展示：插图 + "还没有项目" + 说明文字 + "创建第一个项目"按钮。同时提供"加载示例项目"引导文案。

### 3.11.2 项目内文件树为空

项目已创建但尚未添加任何文件时，文件树区域显示 "+" 图标及"新建文件"文案；右侧编辑器区域同步显示"选择或创建一个文件开始编辑"提示。

### 3.11.3 无运行历史

从未运行过代码时，运行结果区域显示"还没有运行记录" + "点击顶部运行按钮执行你的第一段代码"引导。

### 3.11.4 AI 对话为空

AI 对话面板首次打开时，展示预设引导语："你可以问我这些问题——"，下方提供 3 个示例问题（如"解释这段代码"、"帮我优化性能"、"分析报错原因"），点击后自动发送。

---

# 4，部分痛难点解决方案

## 4.1 前端部分

### 问题一：原项目用 xterm.js 展示实例交互，新项目还要不要保留？

**解决方案：**
主界面不再以 xterm.js 为核心，而改用 **Monaco Editor + 日志展示 + WebSocket 状态流** 作为主交互。

原因是：

* 这个项目核心是“开发实验室”，不是纯命令行练习
* 代码编辑器比终端更符合评委认知
* 日志流、报错高亮、AI 建议联动效果更好

但 **xterm.js 可以保留为“高级模式”或“容器终端入口”，用于进入 shell 调试**也就是说，不是彻底删，而是从主入口降级为辅助功能。原项目中 xterm 的主要作用是和 websocket 直接连接完成实例交互，这一模式仍可作为高级功能保留。

### 问题二：如何实现实时运行反馈？

**解决方案：**
前端通过 WebSocket 接收：

* 任务状态变化
* 日志实时输出
* 容器启动进度
* 资源占用
* AI建议

用户点击“运行”后，页面不刷新，只更新工作台状态。

### 问题三：如何实现多角色权限？

**解决方案：**
保留原项目里“登录后把用户信息存入全局 store，再根据身份动态渲染功能”的思路。
但角色要改成更符合新平台的设计：

* 普通用户：创建项目、运行代码、使用 AI
* 教师/管理员：发布实验模板、查看运行记录、回收资源
* 超级管理员：管理镜像、集群配额、用户权限、审计日志

---
### 前端设计思路仅供参考！！而不是一定要按照这个思路进行
---

## 4.2 后端部分

### 问题一：如何同时支撑“AI 对话”和“代码运行”两类业务？

**解决方案：**
后端拆分为多个微服务，而不是一个大单体。建议至少拆成：

* 用户与权限服务
* 项目与文件服务
* AI 编排服务
* 任务调度服务
* 容器运行服务
* 日志与监控服务
* 评测服务
* 管理后台服务

这样可以避免 AI 请求、运行任务、文件服务互相阻塞。原项目本身就是基于 SpringCloud 的微服务思路搭建管理端，这种拆分方向与原方案一致。

### 问题二：如何管理代码、运行记录、对话记录等不同类型数据？

**解决方案：**
采用 MySQL + MongoDB + Redis + 对象存储 的组合存储。

* MySQL 保存用户、角色、项目元数据、实验模板、资源配额
* MongoDB 保存对话上下文、代码版本快照、任务详情
* Redis 维护任务队列、WebSocket 会话、运行态缓存
* 对象存储服务 存源代码包、运行结果文件、上传数据集

### 问题三：如何让 AI 真正参与开发流程？

**解决方案：**
单独设计一个 **AI 编排服务**，负责：

* prompt 模板管理
* 多模型路由
* 历史上下文压缩
* 代码补全与解释
* 报错分析
* 根据运行结果二次修复建议
* 自动生成测试样例

AI 服务不直接接管业务，而是作为“开发辅助中枢”。

---

## 4.3 K8S 部分

### 问题一：如何为每个用户快速分配隔离的开发环境？

**解决方案：**
采用“模板镜像 + Namespace/标签隔离 + 动态创建 Pod/Job”的模式。

* 轻交互场景：创建短生命周期 Pod
* 批处理场景：创建 Job
* 长时开发场景：创建 Deployment + PVC

每个用户实例附带：

* 用户 ID
* 项目 ID
* 资源上限
* 运行时类型
* 生命周期标签

这样可以实现多租户隔离和实例回收。

### 问题二：如何控制资源滥用，避免比赛演示时集群被跑满？

**解决方案：**
在 K8S 层设置：

* ResourceQuota
* LimitRange
* HPA
* Pod 优先级
* 节点亲和与污点容忍
* TTL 自动回收

同时在后端增加**配额队列**。原项目里已经有“云平台算力队列 + Redis 保存”的思路，用于减少资源浪费，这一点完全可以复用到新平台。

### 问题三：如何判断容器运行失败，到底是镜像问题、容器问题还是应用问题？

**解决方案：**
延续原项目“docker-client + k8s-client 多角度监测资源创建情况”的思路，构建三级诊断：

* 第一级：Pod 调度与拉取镜像异常
* 第二级：容器启动失败或探针失败
* 第三级：应用内部报错、超时、内存溢出、端口未监听

前端统一显示“运行状态树”，并由 AI 自动解释错误原因。原项目也强调了对 Pod、容器、容器内应用状态的区分监测，这一点非常值得保留。

### 问题四：如何实现一键部署和滚动更新？

**解决方案：**
云端继续使用 Helm + CI/CD。原项目把 DevOps、Helm、KubeSphere、Metrics-Server 都作为云端核心技术，这套组合非常适合比赛项目继续沿用。

---

# 可行性结论

这个“AI 云端开发实验室”方案是可行的，而且很适合比赛项目，原因有如下：

第一，它高度继承原 PDF 的成熟架构，不是从零想象，技术迁移路径清晰。原方案已经验证了“前后端 + K8S + 容器动态实例 + 自动化运维”的可行性。

第二，它比普通 AI Web 更有展示价值。评委不仅能看到 AI，还能看到代码编辑、云端运行、容器调度、日志流和资源管理。
